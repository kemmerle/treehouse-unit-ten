import React from 'react';

export default () => (
  /* The NotFound component is a stateless functional component that renders a
  message letting the user know that the requested page can't be found. */
  <div className="bounds">
    <h1>Not Found</h1>
    <p>Sorry! We couldn't find the page you're looking for.</p>
  </div>
);
