import React from 'react';

export default () => (
  /* The UnhandledError component is a stateless functional component that renders
  a message letting the user know that an unexpected error has occurred. */
  <div className="bounds">
    <h1>Error</h1>
    <p>Sorry! We just encountered an unexpected error.</p>
  </div>
);
