import React from 'react';

export default () => (
  /* The Forbidden component is a stateless functional component that renders a
  message letting the user know that they can't access the requested page. */
  <div className="bounds">
    <h1>Forbidden</h1>
    <p>Oh oh! You can't access this page.</p>
  </div>
);
